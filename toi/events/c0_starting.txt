add_namespace = c0_starting

#Default starting event : End of a new beginning

country_event = {
  id = c0_starting.0
  title = c0_starting.0.t
  desc = c0_starting.0.d

  option = {
    name = c0_starting.0.a
    add_political_power = 1
  }
}

country_event = {
  id = c0_starting.1
  #Saga of Two Centuries
  title = c0_starting.1.t

  #Monarchist Description
  desc = {
    text = c0_starting.1.d.1
    trigger = {
      NOT = {
        TAG = ENG
        }
      NOT = {
        TAG = FRA
      }
      NOT = {
        TAG = RAJ
      }

      has_government = {despotic_autocracy}
      NOT = {
        has_ideas = no_heir
      }
    }
  }
  #Kingdom without heir
  desc = {
    text = c0_starting.1.d.2
    trigger = {
      NOT = {TAG = ENG}
      NOT = {TAG = FRA}
      NOT = {TAG = RAJ}
      has_government = despotic_autocracy
      has_ideas = no_heir
    }
  }
  #Democracy
  desc = {
    text = c0_starting.1.d.3
    trigger = {
      NOT = {TAG = ENG}
      NOT = {TAG = FRA}
      NOT = {TAG = SRS}
      OR = {
        has_government = traditional_conservatism
        has_government = market_liberalism
        has_government = social_democracy
      }
    }
  }
  desc = {
    text = c0_starting.1.d.4
    trigger = {
      TAG = ENG
    }
  }
  desc = {
    text = c0_starting.1.d.5
    trigger = {
      TAG = FRA
    }
  }
  desc = {
    text = c0_starting.1.d.6
    trigger = {
      TAG = SRS
    }
  }

  option = {
    name = c0_starting.1.a
  }
}
